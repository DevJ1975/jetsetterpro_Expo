# This folder holds store-submission secrets, referenced by eas.json and the
# /ship-both skill. Everything here except this file is gitignored.
#
# Drop in:
#   asc-api-key.p8                     (App Store Connect API key, iOS)
#   google-play-service-account.json   (Google Play service account, Android)
