// TypeScript 6.0 (Expo SDK 56) stopped auto-including `@types/*` ambient globals
// by default, so jest's globals (describe/it/expect) must be referenced explicitly.
// This keeps the rest of the project's automatic type resolution untouched.
/// <reference types="jest" />
