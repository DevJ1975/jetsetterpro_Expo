// Matches iOS `JetsetterTheme.Card.cornerRadius` (18) and the premium-input
// radius (12).
export const radii = {
  control: 12,  // buttons, inputs
  card: 18,     // cards & tiles
  sheet: 28,    // modals/sheets
  pill: 999,
};
export default radii;
