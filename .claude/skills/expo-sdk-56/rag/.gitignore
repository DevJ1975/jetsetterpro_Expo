# Generated vector index — derived artifact, rebuild with `npm run build`.
# It requires VOYAGE_API_KEY and goes stale as app/ + src/ change, so we don't
# commit it by default. Delete these lines if you'd rather check it in so
# teammates get search without a Voyage key.
index.json
index.*.json

# In case anything ever gets installed here (currently zero deps).
node_modules/

# Auto-rebuild hook artifacts.
rebuild.log
.rebuild.lock/
